/**
 * Central export point for all response formatters
 * Import formatters using: const { UserResponse } = require('./models/responses');
 */

const UserResponse = require('./user.response');

module.exports = {
  UserResponse,
};
